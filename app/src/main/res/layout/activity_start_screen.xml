<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="views.controllers.StartScreen">

    <Button
        android:id="@+id/button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignEnd="@+id/resumeGame"
        android:layout_alignLeft="@+id/resumeGame"
        android:layout_alignParentBottom="true"
        android:layout_alignRight="@+id/resumeGame"
        android:layout_alignStart="@+id/resumeGame"
        android:layout_marginBottom="30dp"
        android:onClick="startGame"
        android:text="Start Game" />

    <Button
        android:id="@+id/resumeGame"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/button"
        android:layout_centerHorizontal="true"
        android:onClick="resumeGame"
        android:text="Resume Game" />

</RelativeLayout>
